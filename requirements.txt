# === 核心 AI 框架 ===
# 锁定 Paddle 版本，防止更新后 API 变动
paddlepaddle==2.6.1
paddleocr==2.7.0.3
# LaMa 修复模型依赖 Torch
torch>=2.0.0
torchvision>=0.15.0

# === 图像处理 ===
# 锁定 Pillow 版本以修复 python-pptx 报错 "AttributeError: module 'PIL.Image' has no attribute 'ANTIALIAS'"
Pillow==9.5.0
# OpenCV 用于图像形态学操作
opencv-python>=4.8.0
# PDF 转图片工具
pdf2image>=1.17.0

# === PPT 生成 ===
python-pptx>=0.6.21

# === AI 修复模型 ===
simple-lama-inpainting>=0.1.0

# === 基础依赖库 ===
# 极其重要：强制锁定 Numpy 在 2.0 以下，防止 Paddle 和 OpenCV 崩溃
numpy<2.0.0
shapely
scikit-image
imgaug